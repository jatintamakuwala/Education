@model IEnumerable<Education.Implementation.ClassMst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassMstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassMstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClassMstID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClassMstID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClassMstID })
        </td>
    </tr>
}

</table>
