@model Education.Implementation.ClassMst

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ClassMst</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ClassMstName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ClassMstName)
            @Html.ValidationMessageFor(model => model.ClassMstName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
